const expect = require("chai").expect;
const forgetPasswordPage = require("../pageobjects/forgetPassword.page");
const inputs = require("../data/forgetPassword.data");
/*
// url
const url = "https://github.com/password_reset";

//
describe("github.com forget password test suite", () => {
  inputs.forEach((input) => {
    it(`${input.desc},  (${input.type} test)`, () => {
      browser.url(url);
      forgetPasswordPage.setEmail(input.email);
      forgetPasswordPage.resetButtonClick();
      expect(forgetPasswordPage.errorMessage).to.contain(input.expectedError);
    });
  });
});
*/
